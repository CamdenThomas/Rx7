# DIAGRAMS — every as-built drawing

*Generated by `tools/rx7.py build` from `data/` and `templates/` — edit those, never this file.*

*Rev 2026-09-08 · owns: the picture index. Every sheet under `systems/*/diagrams/` is rendered here; the sheets themselves are rendered by each system's own build from its tables, so they can never disagree with the tables.*

*No system has completed yet, so no as-built sheet lives here. Project drawings stay with their project until then.*
