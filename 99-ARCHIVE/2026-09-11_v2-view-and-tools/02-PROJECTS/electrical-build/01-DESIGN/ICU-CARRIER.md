# ICU CARRIER — board H-001

*Generated by `tools/rx7.py build` from `data/` and `templates/` — edit those, never this file.*

*Rev 2026-09-08 · owns: the ICU and its display — the instruments of this build. Every channel between `DP-ICU-A` / `DP-ICU-B`, the Teensy 4.1, the display and the radio is a row in `data/icu_channels.csv`; the part numbers are checked against their datasheets (D-273). The pages the display draws are the luxury package's firmware; the bezel around it is the luxury package's dash plastics.*

## What it is

One board behind the binnacle and a 12.3-inch bar in front of it. A Teensy 4.1 socketed on standard 0.1-inch female headers, 8 MB PSRAM on its pads, a TCAN1042 on CAN2, two 12 V inputs, an analog front end for eight sensor lines, three tell-tale senses, a BT817 display bridge on a ribbon, an ESP32-C3 radio for the battery data, and nothing else. The car's instruments are the ICU's from the first drive on the new harness (D-268): it excites the fuel and water-temp senders, reads the PMU-excited oil node, the tach, the charge and brake lines and the three tell-tales, and draws all of it. The factory cluster stays on the factory harness until the meters cutover (MG22) and leaves with it — nothing in the new harness was ever built for it.

## Contents

1. Power and bus · 2. Inputs · 3. The display · 4. On the board — IMU and radio · 5. Grounding and EMC · 6. Bring-up · 7. What changed and why

## 1 · Power and bus

| ID | Signal | At the drop | On the wire | Carrier stage | Teensy pin | Calibration | Can | Note |
|---|---|---|---|---|---|---|---|---|
| IC12 | Logic 12 V | DP-ICU-A 1 ← O10 tap | Accessory bus, 13 A cap shared with the head unit | SS34 → SMBJ33A → LMR36015-Q1-class buck (60 V in) 5 V → Teensy VIN; the radio on its own 3.3 V LDO (D-273) | — | — | — | ~0.8 A peak: the Teensy, the transceiver, the IMU, the three sender excitations, the radio co-processor (≤ 120 mA average, ~300 mA peaks in a BLE scan) — the 36 V LMR33630 would have sat under the clamp's ~53 V |
| IC13 | Aux 12 V, ignition — the display's backlight | DP-ICU-A 6 ← F16 (5 A) | Ignition-switched | SS34 → SMBJ33A → 4 A fuse → ≥ 60 V high-side switch (SQD50P06-class P-FET, or a VN5E-class high-side driver ≥ 6 A) → the display's backlight driver (D-268, D-273) | — | — | — | Never joined to the logic rail on the board — two inputs, two diodes. Backlight current is the panel's spec: ~2.5 A at 12 V for the 900–1000-nit bar and its scaler (D-269), so a 4 A fuse and a switch rated well above it (D-273) |
| IC14 | Illumination | DP-ICU-A 2 ← O20 node | The dash illumination bus, PWM-dimmed by the PMU | Divider → ADC: the dimming reference the display follows | ADC | — | — | The display dims against the O20 bus, so one dimmer runs the whole dash |
| IC15 | Ground | DP-ICU-A 3 → GND bus |  | Single pour, star-tied here | — | — | — |  |

| ID | Signal | At the drop | On the wire | Carrier stage | Teensy pin | Calibration | Can | Note |
|---|---|---|---|---|---|---|---|---|
| IC16 | CAN2 | DP-ICU-A 4 / 5 | 500 kbps; TCAN1042HVDRQ1 confirmed available (V-057 → D-265) | TCAN1042HVDRQ1, 3.3 V IO; no termination on this board (D-079); common-mode choke footprint DNP | CAN1 (FlexCAN) | — | 0x200 · 0x210 · 0x218 out; 0x180 ext (AEMnet, 29-bit, big-endian, 100 Hz) in — D-266 |  |

Two 12 V inputs, never joined on the board: logic on the accessory tap (`DP-ICU-A 1`), the display's backlight on the F16 ignition aux (`DP-ICU-A 6`) through a fuse and a high-side switch the Teensy controls. Each through its own SS34 and SMBJ33A (D-088). The logic buck is an LMR36015-Q1-class 5 V / 3 A; the three sender excitations (100 Ω from 5 V, ≤ 50 mA each at a shorted sender) and the radio co-processor (≤ 120 mA in a scan) hang off it. No CAN termination on this board (D-079).

## 2 · Inputs

| ID | Signal | At the drop | On the wire | Carrier stage | Teensy pin | Calibration | Can | Note |
|---|---|---|---|---|---|---|---|---|
| IC01 | Water temp sender | DP-ICU-B 1 ← L1-S1 3 | 2-wire resistive sender to engine ground; the ICU is its only excitation | 100 Ω 1 W pull-up from the carrier's 5 V behind jumper J1 (fitted) → node → 15 kΩ / 10 kΩ divider (×0.4, 5 V → 2.0 V) → 100 nF → BAT54S to 3V3 / GND | ADC | The car's own C-02 sender stays (D-261): FSM §15 curve checked by a three-point read in the car | 0x200 byte 2 | J1 open + a curve file = a 0–5 V sensor or the ECU's CAN value at the swap (D-262) |
| IC02 | Oil-pressure node | DP-ICU-B 2 ← L1-S1 4 | PMU-excited node: 100 Ω 1 W from pin 15 at the dash node; A7 reads it (D-260) | No pull-up on the carrier — read only: 15 kΩ / 10 kΩ divider → 100 nF → BAT54S | ADC | Same curve as A7's; compare the two readings — a disagreement is a sender or wiring fault | 0x200 bytes 4–5 | The fuel-pump gate is the PMU's on A7, never this input (D-251). At the swap a 0–5 V transducer replaces the sender and the 100 Ω pull-up is lifted at N39 (D-262) |
| IC03 | Fuel level sender | DP-ICU-B 3 ← L4-S 1 | 2-wire resistive sender to the rear star; 6 Ω full · 31.5 Ω mid · 80 Ω empty (D-249) | 100 Ω 1 W pull-up from 5 V behind jumper J3 (fitted) → divider ×0.4 → 100 nF → BAT54S | ADC | D-249's three points, re-read on the tank unit in place (D-261); the display's fuel scale is linearised from them | 0x218 bytes 0–1 | Was the PMU's A7 until D-249; the ICU is the only source now. The tank unit outlives the swap |
| IC04 | Tachometer pulse | DP-ICU-B 4 ← L1-S1 6 (shielded) | Trailing coil negative primary (luxury D-304) — inductive kick, hundreds of volts | 2 × 100 kΩ ½ W series → 5.1 V zener + BAT54S to 3V3 → LM393 on the 5 V rail (its input range stops 1.5 V under its supply — on 3.3 V the zener node would clip) with ~0.5 V hysteresis, 10 kΩ pull-up to 3V3 → input-capture pin | digital, input capture | 2 pulses per revolution; tach_simulator on the bench, then the car | 0x200 bytes 0–1 | D-273 confirms the parts; an H11L1 opto is the alternative if the comparator picks up noise. An ECU tach output (0–12 V square wave) uses the same stage at the swap; pulses per rev is a config value |
| IC05 | Charge-lamp line (alternator L) | DP-ICU-B 5 ← L1-S2 8 | Lamp-drive line: ~0 V when not charging, system volts when charging | 47 kΩ / 10 kΩ divider → RC → BAT54S | ADC | Threshold from the in-car reading, engine off vs running | 0x210 flag | Sense only — the field is excited by F15 → BW (D-198), so no lamp load is needed on L; the display draws the CHARGE tell-tale from this reading |
| IC06 | Brake warning node | DP-ICU-B 6 ← L1-S2 1 + L3-S2 11 | Pulled low by the fluid-level switch or the parking-brake switch | 47 kΩ / 10 kΩ divider → RC → BAT54S | digital | None — low = warn | 0x210 flag | One BRAKE tell-tale for both, as the factory lamp was (luxury D-310) |
| IC07 | Oil temperature | DP-ICU-B 7 — a sealing plug until the swap (D-264) | Future 2-wire NTC sender (Q-308) | Pull-up from 5 V behind jumper J7, sized to the sender's curve (1 kΩ – 2.2 kΩ 1 %), divider ×0.4, RC, BAT54S | ADC | Sender datasheet curve, one-point check in hot oil | 0x200 byte 3 | Footprint only: no oil-temp sender on the 12A and no conductor run for one (D-264); the swap's rebuilt leg lands on DP-ICU-B 7 and the pull-up goes in then (D-262) |
| IC08 | Road speed | DP-ICU-B 8 ← L4-S 3 (LIVE at the cutover); supply on L4-S 4, an O10 tap | Hall pulse generator on the transmission's speedo-drive boss, 12 V square wave, 8 000 or 16 000 pulses per mile (D-272) | Jumper: 4.7 kΩ pull-up to 3V3 (open-collector) or 10 kΩ / 4.7 kΩ divider (push-pull); BAT54S; Schmitt → input capture | digital, input capture | Pulses per km over a measured mile or against GPS | 0x200 byte 6 | D-272: the cable is deleted; the sensor screws onto the boss (P113, DV47) and is fitted at the meters cutover; pulses per mile is a config value, trued once against a measured mile. The same kind of sensor on whatever transmission follows the Jatco, or the ECU's VSS frame if the next box has no drive (engine-swap HO15) |
| IC09 | Turn LEFT tell-tale sense | DP-ICU-B 10 ← tap on the O17 splice | The real lamp output, 0 / 12 V flashing | 47 kΩ / 10 kΩ divider → BAT54S | digital | None | tell-tale | The tell-tale follows the real lamps, not the bus (D-258); the display draws it |
| IC10 | Turn RIGHT tell-tale sense | DP-ICU-B 11 ← tap on the O18 splice | as IC09 | as IC09 | digital | None | tell-tale |  |
| IC11 | High-beam tell-tale sense | DP-ICU-B 12 ← tap on the O3 splice | 0 / 12 V | as IC09 | digital | None | tell-tale |  |
| IC22 | Page button | local, by the display (luxury D-169) | Momentary to ground | 10 kΩ pull-up, 100 nF debounce | digital | — | local | Momentary by the display on the binnacle plate — the plate stays as the bezel's sub-frame (D-269) |

Every analog input ends in the same three parts at the connector edge — series or divider resistor, 100 nF, BAT54S to 3V3 and ground — because the Teensy is 3.3 V only and not 5 V tolerant. The two pulse inputs go to input-capture pins through a comparator or Schmitt stage. **Built to outlive the 12A (D-262):** each sender's pull-up sits behind a jumper (J1, J3, J7 — fitted for the car's own resistive senders, D-261), the ×0.4 divider reads a 0–5 V transducer just as well with the jumper open, and every curve is a config file — at the engine swap the channels change by file and jumper, never by board, and most of them go quiet in favour of the ECU's CAN frames (luxury F-014).

## 3 · The display

| ID | Signal | At the drop | On the wire | Carrier stage | Teensy pin | Calibration | Can | Note |
|---|---|---|---|---|---|---|---|---|
| IC21 | Display header — BT817 EVE bridge to the 12.3-inch bar | on the board, never the harness (luxury D-159); the display is this build's from the first drive (D-268) | QSPI to the BT817 bridge board → TFP410-class HDMI encoder → the 1920 × 720, 900–1000-nit bar's scaler board (chain ii, D-269); the backlight's 12 V from the F16 aux (IC13) | QSPI header to the BT817 board + the backlight feed from IC13; ribbon to the glass through the enclosure grommet | SPI1 / QSPI pins | — | — | Chain ii is decided (V-085 → D-269); T-051 orders the eval board now, V-084 confirms pixel clock and sync polarity against the panel at install §1.22; the pages are the luxury package's firmware |

The 12.3-inch bar is this build's instrument from the meters cutover (D-268). The chain is proved on the desk in order — the BT817 evaluation board first (`T-051`, the one order that should not wait), then the glass (chain ii — the 900–1000-nit 1920 × 720 panel through its scaler, D-269), then the timings (`V-084`, agent work at install §1.22) — and the DRIVE page runs on the bench before the car is touched. It mounts on a plain plate in the binnacle aperture, sized at M-6, which stays as the sub-frame the luxury package's moulded bezel later surrounds (D-269). The odometer starts from the factory cluster's reading at cutover and persists in the Teensy's EEPROM with the microSD as the log (D-269, luxury F-007 — a cutover gate). Road speed comes from the transmission-boss pulse generator (D-272) from the same day, because the mechanical speedometer leaves with the cluster.

## 4 · On the board — IMU and radio

| ID | Signal | At the drop | On the wire | Carrier stage | Teensy pin | Calibration | Can | Note |
|---|---|---|---|---|---|---|---|---|
| IC23 | IMU | on the carrier, I²C | LSM6DSO / ICM-42688-P class (luxury D-109; the MPU-6050 / ICM-20948 are not recommended for new designs — D-273) | I²C + INT; mounted per luxury D-161's axes — V-073 before layout | I²C | Zero at rest on level ground | local — the luxury package's performance page | Fitted now so the board never comes out for it (P109) |

| ID | Signal | At the drop | On the wire | Carrier stage | Teensy pin | Calibration | Can | Note |
|---|---|---|---|---|---|---|---|---|
| IC24 | Radio co-processor — battery data | on the board; nothing at the drops (D-267) | Bluetooth LE to the Ionic's BMS and / or a Victron SmartShunt at the battery (the source is Q-128) | ESP32-C3 module (XIAO ESP32C3 class) on its own 3.3 V LDO (the XIAO's — the Teensy's 250 mA regulator has no headroom for a BLE scan, D-273): UART TX / RX to the Teensy (Serial2), EN and BOOT lines to two GPIOs so the Teensy can reset and reflash it, antenna end overhanging the board edge with a 5 mm keep-out toward an RF-transparent enclosure wall | Serial2 + 2 GPIO | None on this board — the BMS or the shunt calibrates itself; the ICU republishes | 0x220 out (2 Hz) — volts, amps, SoC, temperature, flags | Runs only while the ICU does (the logic rail is O10, accessory): the overnight story is read at key-on from the source's own counters. A dev board is bought now to try the decode (P116) |

The radio is the one addition the battery data asked for (D-267): an ESP32-C3 module on the 3V3 rail talking to the Teensy over a UART, decoding the Ionic's BMS and / or a shunt at the battery (`Q-128` picks the source) — the Teensy has no radio, and keeping the Bluetooth stack on its own chip keeps its timing and its power off the instrument core. The IMU serves the luxury package's performance page; it goes on now so the board never comes out for it (`V-073` → D-265).

## 5 · Grounding and EMC

Single pour, star-tied at `DP-ICU-A 3`. Analog inputs enter on one edge; CAN, power, the display header and the radio leave on the other (D-265); every RC lives at the connector edge. The tach shield lands at the dash node end only (node conductor N48). The display header's QSPI lines stay short and away from the tach input. The ESP32-C3's antenna end overhangs the board edge with a 5 mm keep-out and faces an RF-transparent enclosure wall — the printed ASA / PETG enclosure of D-270, no external antenna needed; the module sits at the CAN / display edge, as far from the tach comparator as the board allows.

## 6 · Bring-up

The bench sequence is the install plan's, not this document's: [`../03-INSTALL/INSTALL.md`](../03-INSTALL/INSTALL.md) §1.21–1.23 — power with nothing fitted, then the Teensy and CAN, then each input against a resistor or the tach simulator, then the display chain (eval board, glass, timings) with the DRIVE page live and the odometer persisting across a power cycle. Only then the car.

## 7 · What changed and why

Until 2026-09-07 this board was the luxury package's, read the senders as a high-impedance observer of nodes the factory gauges drove, and shared those nodes with a cluster drop (`DP-CLU`). Camden ruled the ICU a wiring-project device (Q-122 → D-259) and the cluster temporary and traceless (Q-121 → D-258): the senders came to the ICU and the drop went. The same evening he asked whether the display should simply come now, with only the dash plastics left to the luxury package (Q-129 → D-268) — and it should, because the parallel-system migration already keeps the factory cluster alive on the factory harness until its own cutover, so the temporary cluster, its twelve-line tail and the three gauge-drive stages that D-258 had put on this board were never needed. The oil node stays PMU-excited so the fuel-pump gate never depends on this board (D-260).
