# 2026-09-03 — the data migration (D-233)

The three scripts here parsed the hand-maintained documents of `02-PROJECTS/electrical-build` and `00-CAR` into `data/*.csv` + `templates/*.md` for `tools/rx7.py`, once. They are kept for the record and will not run again (the documents they read no longer exist in that shape). The two `*-2026-08-31.md` files are the root `ASSISTANT.md` and `README.md` as they stood before the rewrite.
