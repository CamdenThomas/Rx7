**D-263 — The fuel, water-temperature and oil-pressure gauges are bimetal, so the ICU's gauge drives are 1–2 kHz PWM low-side sinks and the snubber values in `icu_channels` (IC17–IC19) are final.** Camden, 2026-09-07, answering `Q-123`: all three are bimetal. A heater strip behind the needle integrates anything above a few hertz, so the drive frequency is a firmware constant, not a layout question, and the board is free to be ordered once the enclosure is settled (`Q-126`). The bench calibration (install §1.22) allows ten seconds per point — a bimetal needle is slow by design.
